<Page x:Class="PL.View.Menue"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PL.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Menue">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TabControl x:Name="MainTabs" Grid.Column="1" Width="800"/>
        <WrapPanel Orientation="Vertical" Grid.Column="0" Background="{Binding ElementName=stationList, Path=Background}">
            <Button Content="{Binding ButtonA_Content}" Command="{Binding ButtonA_Command}"/>
            <Button Content="{Binding ButtonB_Content}" Command="{Binding ButtonB_Command}"/>
            <Button Content="{Binding ButtonC_Content}" Command="{Binding ButtonC_Command}"/>
            <Button Content="{Binding ButtonD_Content}" Command="{Binding ButtonD_Command}"/>
            <Button Content="back" Command="{Binding CloseCd}"/>
        </WrapPanel>
    </Grid>
</Page>
